# Development baseline — 2026-08-10

This checkpoint preserves user-owned work that pre-dated the HTTP hub
migration. No OmniFocus write was performed while establishing this baseline.

## Existing changes

- Added a local `registerTool` wrapper to contain TypeScript/Zod inference and
  explicit `CallToolResult` return types across tool handlers.
- Added JSON-first success/error formatting for mutation handlers and changed
  `get_inbox_tasks` to return its raw task array as JSON.
- Added local ESM import repair and direct/MCP diagnostic scripts.
- Adjusted the move-task test for the wider MCP content type and suppressed a
  TypeScript deprecation diagnostic.

## Verification

- `npm run build`: passed.
- `node --test dist/**/*.test.js`: 58 passed, 0 failed.
- `npm test`: passed, but is only the repository's manual-test placeholder.
- `test_mcp.mjs`: deliberately not run because it calls
  `add_omnifocus_task` and would perform a real OmniFocus write.

## Known risks

- `test_mcp.mjs` contains development-only placeholder URLs/tokens and is not
  safe for unattended execution.
- The JSON response migration is broad and should receive separate consumer
  compatibility review; the HTTP transport migration should not expand it.
